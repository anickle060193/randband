<% provide( :title, "Edit Band" ) %>

<div class="row">
  <div class="col-md-6 col-md-offset-3">

    <div class="panel-heading">
       <div class="panel-title text-center">
          <h1 class="title">Edit Band</h1>
          <hr>
        </div>
    </div>

    <% provide( :button_text, "Update Band" ) %>
    <%= render 'form' %>

  </div>
</div>